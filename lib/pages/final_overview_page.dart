import 'package:flutter/material.dart';

class FinalOverviewPage extends StatelessWidget {
  const FinalOverviewPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Final Overview page"),
    );    
  }
}