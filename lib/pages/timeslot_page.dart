import 'package:flutter/material.dart';

class TimeslotPage extends StatelessWidget {
  const TimeslotPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Timeslot page"),
    );    
  }
}